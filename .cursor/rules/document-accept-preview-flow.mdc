---
description: Document Accept flow — WhatsApp-only preview PDF → I Accept → original. Do not invent a web Accept page.
alwaysApply: true
---

# Document Accept flow (WhatsApp + secure email)

**Status:** Implemented locally (HydrogenRO). Test locally; do **not** merge to main / Netlify-deploy unless asked.

## Agreed product flow

1. CRM generates a **PREVIEW** PDF (watermarked “PREVIEW – NOT VALID”).
2. WhatsApp sends preview PDF + **I Accept** (quick reply) — **normal flow in all CRM Require Accept places** (`document-accept-send` → `createAndSendAcceptInvite`):
 - **Inside 24h:** free-form interactive (DOCUMENT + terms + I Accept).
 - **Outside 24h (or interactive fail):** cold template `svc_doc_accept_preview_{hro|ero}_v8` (fallback **v7** only). Do **not** try interactive first when the window is closed (Meta may 200 then fail with Re-engagement).
3. Customer taps **I Accept** on WhatsApp → webhook `document-accept-inbound`.
4. Server validates invite → saves audit → status `accepted` (single-use claim).
5. Send **ORIGINAL** PDF on same WhatsApp chat (seal + authenticity hash + confirmation id).
6. **Do not store PDF bytes forever** — original briefly on private R2 (`whatsapp/accept/…`), deleted after accept; DB keeps hash + audit only.

### Email flow

1. CRM emails the watermarked **PREVIEW** PDF plus a secure **Review & Accept** link.
2. Link opens `/accept/{opaque-token}`; token plaintext is emailed once and only its SHA-256 hash is stored.
3. Customer explicitly checks the agreement box and taps **I Accept**.
4. Public Netlify function validates, rate-limits, and atomically claims the single-use invite server-side.
5. Server emails the verified **ORIGINAL** PDF to the same address, records confirmation/audit, and deletes the private R2 object.
6. Email, WhatsApp, and Both are available in all active CRM **Require Accept** document dialogs.

## Do not use

- Old APPROVED `svc_doc_accept_preview_*_v1`–`v4` (URL Accept → `/c/{{1}}`) — wrong UX; not shipped.
- Web Accept lookup/confirm page as the primary **WhatsApp** path. `/accept/{token}` is for email invitations only.

## Implementation map

| Piece | Location |
|-------|----------|
| SQL | `scripts/add-document-accept-invites.sql` (**run in Supabase**) |
| Admin send | `document-accept-send.js` |
| Inbound Accept | `document-accept-inbound.js` (webhook) |
| Helpers | `document-accept-helper.js` |
| Email send | `document-accept-email-send.js` + `document-accept-email-helper.js` |
| Public email Accept | `document-accept-public.js` + `PublicDocumentAcceptPage.tsx` |
| CRM UI | Document email/WhatsApp dialog → **Require Accept** checkbox |
| Client | `src/lib/documentAcceptPreview.ts` |
| Cold names | `resolveDocAcceptPreviewTemplate` → `*_v8`; cold-fallback `v8`→`v7` only |

## Direct PDF (no Accept) — use for normal WhatsApp PDF sends

Separate from Accept. Letter + light emojis; DOCUMENT header; Call us + Website.

| Kind | Eleven | Hydrogen |
|------|--------|----------|
| Any label | `svc_doc_direct_ero_v1` | `svc_doc_direct_hro_v1` |
| Per-kind | `svc_doc_{bill\|invoice\|amc\|…}_ero_v3` | `svc_doc_*_hro_v3` |

- Direct: `{{1}}` name, `{{2}}` doc label (“AMC agreement”, …).
- Per-kind v3: `{{1}}` name only; fixed copy per type.
- CRM: `sendColdDocumentInvite` / `resolveDirectDocTemplate` / `resolveColdDocTemplateName` — **never** accept-preview for normal sends.
- Submit: `node scripts/submit-whatsapp-full-utility.mjs --submit --only-doc-direct`

## Accept-preview Meta templates (WhatsApp-only)

| Brand | Name |
|-------|------|
| Eleven | `svc_doc_accept_preview_ero_v8` (fallback `…_v7`) |
| Hydrogen | `svc_doc_accept_preview_hro_v8` (fallback `…_v7`) |

- DOCUMENT header (preview PDF) + body (name + doc label) + **Call us** + **I Accept** quick reply.
- Until Meta APPROVES v7/v8, cold Accept outside 24h fails; in-window interactive still works.
- CRM helpers: `resolveDocAcceptPreviewTemplate`, `docAcceptPreviewBodyParams` in `src/lib/whatsappColdTemplates.ts`.
- Submit: `node scripts/submit-whatsapp-full-utility.mjs --submit --only-doc-accept`

## Security (required when building)

- Opaque cryptographically random **single-use** token. WhatsApp uses an invite-bound quick reply; email stores only the URL token hash.
- Token tied to one document + one customer + one WA number; expire ~48h.
- Server-side Accept only (webhook); never trust client amount/name/hash.
- Soft-fail WhatsApp paths where appropriate; RLS on tables.
- No OTP in v1 (WhatsApp delivery is enough).

## Audit to store on Accept

`document_id`, `customer_id`, WA number, PDF SHA-256 (original), version, confirmation timestamp, IP/UA if appropriate, unique confirmation id, status, WA message id if available.

## Out of scope / do not do

- Require drawn signature.
- Treat print/screenshot of preview as original.
- Skip authenticity fingerprint on the **original** PDF send (keep existing hash + verify code rules).
- Merge to main / Netlify deploy unless user asks.
- Rebuild web `/c/{token}` Accept as the default path unless the user explicitly asks.

## Build trigger

User says e.g. “build document accept” / “Require Accept”. Wire send after job/doc finalize via CRM checkbox + helpers above.
