<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Native FCM handler: turns the nightly cash-check data push into a
             Yes/No notification even when the app is killed. Replaces the
             Capacitor plugin's service (which it extends, so normal push
             behavior is unchanged). -->
        <service
            android:name=".HroMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />

        <!-- Handles the Yes/No buttons on the cash-check notification -->
        <receiver
            android:name=".CashCheckReceiver"
            android:exported="false" />

        <!-- Nightly expense review Yes/No → Payments add expense -->
        <receiver
            android:name=".ExpenseReviewReceiver"
            android:exported="false" />

        <!-- Morning reminder / pending-payment push (WhatsApp + Open actions) -->
        <receiver
            android:name=".ReminderPushReceiver"
            android:exported="false" />

        <!-- Inline reply when a technician answers an office message -->
        <receiver
            android:name=".TechMessageReplyReceiver"
            android:exported="false" />

        <!-- Caller lookup: saves the incoming number locally so the dashboard
             auto-searches that customer on next open. PHONE_STATE is a
             protected system broadcast, so exported=true is safe. -->
        <receiver
            android:name=".CallCaptureReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Branded push notifications: white logo silhouette + accent color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notify" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_accent" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Job / customer photos: WebView getUserMedia + file input capture -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Gallery pick (image/* file input) on Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Gallery pick on Android 12 and below -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Legacy PDF save to public Downloads (API ≤28). Android 10+ uses MediaStore. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- navigator.geolocation in the WebView (distance Calculate button) needs these;
         Capacitor's WebChromeClient requests them at runtime when the page asks for location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- navigator.vibrate in the WebView (button haptics) is silently ignored without this -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Fingerprint / face unlock (Admin app lock) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Caller lookup: detect ringing + read the incoming number
         (Android 9+ only includes the number when READ_CALL_LOG is granted) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
</manifest>
